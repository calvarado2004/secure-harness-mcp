# Values here are documentation, not credentials. This file is safe to commit and the
# lane must stay silent on it for that reason.
DATABASE_URL=<your-connection-string>
JWT_SECRET=change-me
API_KEY=placeholder
PORT=3000
